# MCP Launcher Requirements

# Core MCP framework
# fastmcp 3.4+ floors starlette at >=1.0.1 (required by fastmcp-slim[server]).
# Our custom DualHeaderVerifier + server_factory.py are verified against 1.x.
fastmcp>=3.4.2
mcp>=1.28.0

# Web server
uvicorn[standard]>=0.30.0
# starlette>=1.0.1 is forced by fastmcp-slim[server]>=3.4. Pin the floor so a
# fresh install doesn't accidentally land on the old 0.x line.
starlette>=1.0.1
fastapi>=0.104.0

# HTTP client
httpx>=0.27.0

# Management UI (NiceGUI)
nicegui>=1.4.0
aiohttp>=3.9.0

# WebSocket support
websockets>=12.0

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Database support (for SQLite persistence)
aiosqlite>=0.19.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Monitoring and observability
prometheus-client>=0.19.0
structlog>=23.2.0

# Utilities
click>=8.1.0
rich>=13.7.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Tool-specific dependencies (optional, depending on which tools you use)
# oracledb>=1.0.0
qdrant-client>=1.7.0        # memorymcp + ragmcp vector backend (Phase 2)
# beautifulsoup4>=4.12.0
# serpapi>=0.1.0

# Turso / libSQL backend (Phase 3) — single embedded process, no containers
libsql-experimental>=0.0.30

# PostgreSQL backend (used by PostgresSqlStore + PostgresVectorStore/pgvector)
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# Optional: Redis for distributed deployment
# redis>=5.0.0
# hiredis>=2.2.0
