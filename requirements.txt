# MCP Launcher Requirements

# Core MCP framework
# fastmcp 4 (2026-07-28 protocol era) — stable 4.0.0 (PyPI 2026-08-31), bumped from
# the 4.0.0b3 beta of the 2026-08-24 user-approved early rollout. Validated per the
# step-9 follow-up: Q6 session-flush check + full suite green on 4.0.0 in a fresh
# venv (plans/mcp-2026-07-28-stateless-upgrade.md).
# fastmcp 4 pulls mcp 2.x + starlette 1.x + pydantic 2.x transitively and no longer
# depends on fastapi (fastapi below stays for the launcher's FastAPI-tool validation).
fastmcp==4.0.0
mcp==2.0.0

# Web server
uvicorn[standard]>=0.30.0
# starlette>=1.0.1 is forced by fastmcp-slim[server]. Pin the floor so a
# fresh install doesn't accidentally land on the old 0.x line.
starlette>=1.0.1
# fastapi is OUR direct dep (launcher validation tier), not fastmcp's anymore.
# Floor 0.133 = first line that tolerates starlette>=1.0.
fastapi>=0.133.0

# HTTP client
httpx>=0.27.0

# Management UI (NiceGUI)
nicegui==3.16.0
aiohttp>=3.9.0

# WebSocket support
websockets>=12.0

# Configuration
# pydantic 2.12 is fastmcp 4's floor (stable 4.0.0 resolves pydantic 2.13.x transitively)
pydantic>=2.12.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Database support (for SQLite persistence)
aiosqlite>=0.19.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Monitoring and observability
prometheus-client>=0.19.0
structlog>=23.2.0

# Utilities
click>=8.1.0
rich>=13.7.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Tool-specific dependencies (optional, depending on which tools you use)
# oracledb>=1.0.0
qdrant-client>=1.7.0        # memorymcp + ragmcp vector backend (Phase 2)
# beautifulsoup4>=4.12.0
# serpapi>=0.1.0

# Turso / libSQL backend (Phase 3) — single embedded process, no containers
libsql-experimental>=0.0.30

# PostgreSQL backend (used by PostgresSqlStore + PostgresVectorStore/pgvector)
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# Optional: Redis for distributed deployment
# redis>=5.0.0
# hiredis>=2.2.0
