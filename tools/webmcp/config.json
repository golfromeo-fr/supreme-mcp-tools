{
  "name": "webmcp",
  "version": "1.0.0",
  "description": "Enhanced Web MCP Server - Provides web search, URL fetching, and content processing capabilities",
  "transports": {
    "sse": {
      "enabled": true,
      "script": "webmcp.py",
      "port": 8001,
      "endpoint": "/sse"
    },
    "streamable-http": {
      "enabled": true,
      "script": "webmcp_streamable.py",
      "port": 8001,
      "endpoint": "/mcp",
      "framing_format": "newline-delimited"
    }
  },
  "tools": [
    {
      "name": "brave_search_web",
      "description": "Enhanced web search using Brave Search with language support and metadata (web scraping). Returns formatted HTML results."
    },
    {
      "name": "brave_search_api",
      "description": "Enhanced web search using Brave Search API with structured results. Returns formatted JSON results including web results, news, videos, discussions, infobox, FAQ, and locations. Supports both GET and POST methods."
    },
    {
      "name": "google_search_api",
      "description": "Google Search API using SerpAPI. Returns formatted results including organic results, answer box, knowledge graph, people also ask, related searches, top stories, and local results."
    },
    {
      "name": "fetch_url",
      "description": "Enhanced web reader that fetches and processes web content with pagination, caching, and content filtering. Supports auto-detection of response type (json/html/text)."
    },
    {
      "name": "post_url",
      "description": "Sends an HTTP POST request to the specified URL with optional data and headers. Supports JSON payloads and returns the response as text."
    }
  ],
  "environment_variables": {
    "BRAVE_SEARCH_API_KEY": {
      "description": "Brave Search API key for structured web search results",
      "required": false,
      "secret": true,
      "default": ""
    },
    "SERPAPI_API_KEY": {
      "description": "SerpAPI key for Google Search API results",
      "required": false,
      "secret": true,
      "default": ""
    },
    "WEBMCP_MAX_RESULTS": {
      "description": "Default maximum number of search results to return",
      "required": false,
      "secret": false,
      "type": "integer",
      "default": "10",
      "minimum": 1,
      "maximum": 100
    },
    "WEBMCP_SAFE_SEARCH": {
      "description": "Safe search level for Brave Search",
      "required": false,
      "secret": false,
      "type": "string",
      "default": "active",
      "options": ["off", "moderate", "active"]
    },
    "WEBMCP_COUNTRY": {
      "description": "Default country code for search results",
      "required": false,
      "secret": false,
      "default": "US"
    },
    "WEBMCP_LANGUAGE": {
      "description": "Default language code for search results",
      "required": false,
      "secret": false,
      "default": "en"
    },
    "MCP_CACHE_MAX_SIZE": {
      "description": "Maximum number of cache entries",
      "required": false,
      "secret": false,
      "type": "integer",
      "default": "1000",
      "minimum": 1
    },
    "MCP_CACHE_TTL": {
      "description": "Cache TTL in seconds",
      "required": false,
      "secret": false,
      "type": "integer",
      "default": "300",
      "minimum": 0
    },
    "MCP_CACHE_ENABLED": {
      "description": "Enable or disable caching",
      "required": false,
      "secret": false,
      "type": "string",
      "default": "true",
      "options": ["true", "false"]
    }
  },
  "auth": {
    "api_key": "webmcp-test-key-1234"
  }
}
