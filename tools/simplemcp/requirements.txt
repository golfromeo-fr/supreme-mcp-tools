# Simple MCP 8 Server Dependencies

# Core MCP framework
mcp>=1.0.0
anyio>=4.0.0
click>=8.0.0

# Web server (SSE transport)
starlette>=0.27.0
uvicorn>=0.27.0

# Web server (Streamable HTTP transport)
fastapi>=0.104.0
